/**
 * @file
 * decoupleddays behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.decoupleddays = {
    attach (context, settings) {

      // console.log('It works!');

    }
  };

} (Drupal));
